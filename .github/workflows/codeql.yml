name: CodeQL

on:
  schedule:
    - cron: '15 06 * * MON'

jobs:
  run-codeql:
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: navikt/dok-workflows/.github/workflows/codeql.yml@main
    secrets: inherit